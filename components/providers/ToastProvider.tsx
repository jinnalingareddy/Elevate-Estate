// Re-exports the ToastProvider and useToast hook from the UI layer.
// Import from here in app layouts so the import path stays consistent.
export { ToastProvider, useToast } from "@/components/ui/Toast";
